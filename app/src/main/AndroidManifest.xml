<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCal">

        <activity android:name=".activities.BasicCalculatorActivity" />
        <activity android:name=".activities.AgeCalculatorActivity" />
        <activity android:name=".activities.CurrencyConverterActivity" />
        <activity android:name=".activities.EquationSolverActivity" />
        <activity android:name=".activities.SelectEquationDegreeActivity" />
        <activity android:name=".activities.LengthConverterActivity" />
        <activity android:name=".activities.BmiBmrCalculatorActivity" />
        <activity android:name=".activities.DerivativeCalculatorActivity" />
        <activity android:name=".activities.IntegralCalculatorActivity" />



        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>